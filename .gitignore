node_modules
.next
out
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel
.superpowers

.env*

# Momen tamu tersimpan di sini saat dev di local (bukan Vercel Blob) —
# lihat lib/moments.ts. Data uji coba lokal, tidak perlu ikut ke-commit.
public/moments-local

# Salinan kerja lib/repo/json-file.ts — dibuat otomatis dari data/seed/
# saat pertama dibaca (lihat lib/repo/json-store.ts). data/seed/*.json
# yang ikut git, bukan ini; folder ini isinya hasil tulis admin lokal.
/data/*.json
